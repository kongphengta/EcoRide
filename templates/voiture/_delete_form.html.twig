<form method="post" action="{{ path('app_voiture_supprimer', {'id': voiture.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette voiture ?');" style="display: inline-block;">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ voiture.id) }}">
	<button class="btn btn-sm btn-danger">Supprimer</button>
</form>
