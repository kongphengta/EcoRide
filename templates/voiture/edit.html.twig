{% extends 'base.html.twig' %}

{% block title %}Modifier la voiture
{% endblock %}

{% block body %}
	<div class="container mt-4">
		<h1>Modifier la voiture</h1>

		{{ include('voiture/_form.html.twig', {
        'button_label': 'Mettre à jour'
    }) }}

		<a href="{{ path('app_voiture_index') }}" class="btn btn-secondary mt-3">
			<i class="fas fa-arrow-left me-2"></i>Retour à la liste</a>
	</div>
{% endblock %}
