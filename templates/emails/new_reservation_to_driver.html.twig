<!DOCTYPE html>
<html lang="fr">
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>Nouvelle demande de réservation</title>
		<style>
			body {
				font-family: Arial, sans-serif;
				line-height: 1.6;
				color: #333;
				max-width: 600px;
				margin: 0 auto;
				padding: 20px;
			}
			.header {
				background: #007bff;
				color: white;
				padding: 20px;
				text-align: center;
				border-radius: 8px 8px 0 0;
			}
			.content {
				background: #f8f9fa;
				padding: 30px;
				border-radius: 0 0 8px 8px;
			}
			.info-box {
				background: white;
				padding: 20px;
				border-radius: 8px;
				margin: 20px 0;
				border-left: 4px solid #007bff;
			}
			.btn {
				display: inline-block;
				background: #007bff;
				color: white !important;
				padding: 12px 24px;
				text-decoration: none;
				border-radius: 6px;
				margin: 10px 5px;
			}
			.btn-success {
				background: #28a745;
			}
			.btn-danger {
				background: #dc3545;
			}
			.footer {
				margin-top: 30px;
				padding-top: 20px;
				border-top: 1px solid #ddd;
				font-size: 14px;
				color: #666;
			}
		</style>
	</head>
	<body>
		<div class="header">
			<h1>🚗 Nouvelle demande de réservation</h1>
		</div>

		<div class="content">
			<p>Bonjour
				{{ reservation.covoiturage.chauffeur.firstname }},</p>

			<p>Vous avez reçu une nouvelle demande de réservation pour votre trajet !</p>

			<div class="info-box">
				<h3>👤 Informations du passager</h3>
				<p>
					<strong>Nom :</strong>
					{{ reservation.passager.firstname }}
					{{ reservation.passager.lastname }}</p>
				<p>
					<strong>Pseudo :</strong>
					{{ reservation.passager.pseudo }}</p>
				{% if reservation.passager.telephone %}
					<p>
						<strong>Téléphone :</strong>
						{{ reservation.passager.telephone }}</p>
				{% endif %}
			</div>

			<div class="info-box">
				<h3>📍 Détails de la réservation</h3>
				<p>
					<strong>Trajet :</strong>
					{{ reservation.covoiturage.lieuDepart }}
					→
					{{ reservation.covoiturage.lieuArrivee }}</p>
				<p>
					<strong>Date :</strong>
					{{ reservation.covoiturage.dateDepart|date('d/m/Y à H:i') }}</p>
				<p>
					<strong>Nombre de places demandées :</strong>
					{{ reservation.nbPlacesReservees }}</p>
				<p>
					<strong>Prix total :</strong>
					{{ reservation.nbPlacesReservees * reservation.covoiturage.prixPersonne }}€</p>
			</div>

			<p>Vous pouvez accepter ou refuser cette demande en vous rendant sur la page de gestion de vos passagers :</p>

			<a href="{{ url('app_covoiturage_passengers', {'id': reservation.covoiturage.id}) }}" class="btn">Gérer mes passagers</a>

			<div class="footer">
				<p>Prenez le temps d'examiner le profil du passager avant de prendre votre décision. 🤝</p>
				<p>
					<em>Cet email a été envoyé automatiquement, merci de ne pas y répondre.</em>
				</p>
			</div>
		</div>
	</body>
</html>
