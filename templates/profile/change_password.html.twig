{% extends 'base.html.twig' %}

{% block title %}Changer mon mot de passe
{% endblock %}

{% block body %}
	<div class="container mt-5">
		<div class="row justify-content-center">
			<div class="col-md-6">
				<div class="card shadow-sm">
					<div class="card-header">
						<h1 class="h3 mb-0">Changer mon mot de passe</h1>
					</div>
					<div class="card-body">
						{{ form_start(changePasswordForm) }}
						{{ form_widget(changePasswordForm) }}
						<button class="btn btn-primary mt-3 w-100" type="submit">Mettre à jour le mot de passe</button>
						{{ form_end(changePasswordForm) }}
					</div>
				</div>
				<div class="text-center mt-3">
					<a href="{{ path('app_profile') }}">Retour au profil</a>
				</div>
			</div>
		</div>
	</div>
{% endblock %}
