{% extends 'base.html.twig' %}

{% block title %}Modifier mes informations
{% endblock %}

{% block body %}
	<div class="container mt-5">
		<div class="row justify-content-center">
			<div class="col-md-8">
				<div class="card shadow-sm">
					<div class="card-header">
						<h1 class="h3 mb-0">Modifier mes informations</h1>
					</div>
					<div class="card-body">
						<p>Utilisez ce formulaire pour mettre à jour vos informations personnelles.</p>

						{{ form_start(profileForm) }}
						{{ form_widget(profileForm) }}
						<button class="btn btn-primary mt-3 w-100" type="submit">Enregistrer les modifications</button>
						{{ form_end(profileForm) }}
					</div>
				</div>
				<div class="text-center mt-3">
					<a href="{{ path('app_profile') }}">Retour au profil</a>
				</div>
			</div>
		</div>
	</div>
{% endblock %}
